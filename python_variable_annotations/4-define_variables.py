#!/usr/bin/env python3
"""This module contains a floor function that takes
a float n as argument and returns the floor of the float."""
import math


def floor(n: float) -> int:
    """Returns the floor of a few variables types."""
    return math.floor(n)


a: int = 1
pi: float = 3.14
i_understand_annotations: bool = True
school: str = "Holberton"
